openapi: 3.0.3
info:
  title: 'Laravel API Documentation'
  description: ''
  version: 1.0.0
servers:
  -
    url: 'http://localhost'
tags:
  -
    name: Login
    description: Login
paths:
  /api/v8/authenticate:
    get:
      summary: Login
      operationId: login
      description: 'Returns a token.'
      parameters: []
      responses:
        200:
          description: ''
          content:
            application/json:
              schema:
                type: object
                example:
                  token: token
                properties:
                  token:
                    type: string
                    example: token
      tags:
        - Login
